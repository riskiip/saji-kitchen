import { FadeinAnimationDirective } from './fadein-animation.directive';

describe('FadeinAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeinAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
